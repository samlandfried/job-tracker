All Jobs for <strong><%= @company.name %></strong>

<%= button_to "Add New Job", new_company_job_path(@company), method: 'get' %>

<% @jobs.each do |job| %>
  <ul><%= link_to job.title, company_job_path(@company, job) %> at <%= @company.name %></ul>
<% end %>
