<h1>New Category</h1>

<% if @category.errors.any? %>
<h2><%= pluralize @category.errors.count, "error" %> prevented this Category from being created.</h2>
<ul>
<% @category.errors.full_messages.each do |error| %>
  <li>
    <%= error %>
  </li>
  <% end %>
</ul>
<% end %>

<%= render partial: 'form' %>